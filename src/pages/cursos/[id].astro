---
import Layout2 from "../../layouts/Layout2.astro";
import Navbar from "../../components/Navbar";
import Section1 from "../../components/Section1.astro";
import Hero from "../../components/Hero";

import CursosDestacados from "../../components/CursosDestacados.astro";
import Section3 from "../../components/Section3.astro";
import FondoHero from "../../components/FondoHero.astro";

import Footer from "../../components/Footer.astro";
import { dataCursos } from "../../data/courses.ts";

const {id}=Astro.params;
const redirect = dataCursos.some((curso) => Number(curso.id) == Number(id));
// console.log(typeof(curso.id), curso.id);
const curso = dataCursos.find((curso) => Number(curso.id) == Number(id));

console.log(typeof(id),id);
console.log(redirect);

if (!redirect) {
  return Astro.redirect("/404");
}
// Astro.redirect("/404",404);

---



<Layout2 title={`Curso ${curso?.title}`}>


	<Navbar slot="navbar" client:only="react" />


	<div class="mx-auto max-w-screen-xl px-4 py-16 sm:px-6 sm:py-24 lg:px-8">

		<!-- Card con margen y padding mejorado para una visualización más limpia -->
		<div class="card lg:card-side bg-base-100 shadow-xl mb-8 lg:mb-16">

			<figure >
				<img src="https://daisyui.com/images/stock/photo-1494232410401-ad00d5433cfa.jpg" alt="Album"/></figure>
		  <div class="card-body">
			<h2 class="card-title">New album is released!</h2>
			<p>Click the button to listen on Spotiwhy app.</p>
			<div class="card-actions justify-end">
			  <button class="btn btn-primary">Listen</button>
			</div>
		  </div>
		</div>


  <!-- Contenedor Principal -->
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
	<!-- Fila con distribución de columnas -->
	<div class="flex flex-wrap -mx-3">
	<!-- Columna Izquierda -->
	<div class="w-full md:w-1/2 px-3 mb-6 md:mb-0">
	<div class="border border-dashed  bg-base-300 border-gray-400 rounded-box h-64">
	  <!-- Contenido de la columna izquierda -->
	</div>
	</div>
	<!-- Columna Derecha -->
	<div class="w-full md:w-1/2 px-3">
	<div class="border border-dashed  bg-base-300 border-gray-400 rounded-box h-64">
	  <!-- Contenido de la columna derecha -->
	</div>
	</div>
	</div>
  </div>


	  </div>


	<Footer slot="footer" />
</Layout2>
